{
  "name": "pkays-portfolio-builder",
  "version": "1.0.0",
  "description": "Hybrid CV & Field Portfolio Builder",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static HTML - no build needed'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
